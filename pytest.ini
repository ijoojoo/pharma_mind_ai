# file: pytest.ini
# purpose: pytest 基础配置（启用 Django 环境、标记、最小日志）
[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests/test_*.py
addopts = -q -ra --disable-warnings
log_cli = False
filterwarnings =
    ignore::DeprecationWarning