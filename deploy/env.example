# file: deploy/env.example
# purpose: 运行所需环境变量示例（docker run --env-file 使用）；请按需修改
DJANGO_SETTINGS_MODULE=config.settings
SECRET_KEY=please_change_me
DEBUG=False
ALLOWED_HOSTS=*
# 数据库（PostgreSQL 示例）
DATABASE_URL=postgres://postgres:postgres@db:5432/pharma
# Caches
CACHE_URL=redis://redis:6379/1
# AI 默认 Provider（可为空，系统内部有回退）
LLM_PROVIDER=gpt
OPENAI_API_KEY=sk-xxxxx
# 日志/版本
APP_VERSION=1.0.0
ENV=prod
# 中间件参数（见 settings_ai.py 中注释）
AI_METRICS_ENABLED=True
AI_SLOW_MS=1500